# ========================================
# RAG 시스템 추가 패키지
# ========================================
# 사용자가 수동으로 설치해야 합니다:
# pip install -r rag_requirements.txt
#
# 주의: 기존 requirements.txt에 있는 패키지는 제외
# (openai, chromadb, tiktoken, rich는 이미 설치됨)

# PDF 처리
PyMuPDF>=1.23.0
pdfplumber>=0.10.0

# 한국어 임베딩
sentence-transformers>=2.2.0

# LangChain
langchain>=0.1.0
langchain-community>=0.0.10
langchain-text-splitters>=0.0.1

# 이미지 처리
Pillow>=10.0.0

