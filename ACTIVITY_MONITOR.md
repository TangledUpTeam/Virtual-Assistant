# Activity Monitor (동작 감지)

## 🎯 기능

사용자 입력(마우스/키보드)을 감지하여 자동 반응

### 1. Idle 감지
- **Dev**: 3초 입력 없음
- **Prod**: 5분 입력 없음
- **동작**: 채팅창에 "조금 쉬고 계신가요? 😊" + Idle 모션

### 2. 장시간 활동 감지
- **Dev**: 10초 연속 활동
- **Prod**: 50분 연속 활동 (5분 미만 휴식은 같은 세션)
- **동작**: 채팅창에 "스트레칭 어때요? 🤸‍♀️" + Tap@Body 모션

---

## ⚙️ 설정 변경

`index.html` 348번째 줄:
```javascript
const mode = 'dev';  // 또는 'prod'
```

### 시간 변경
`index.html` 240-250번째 줄:
```javascript
const ACTIVITY_CONFIG = {
  dev: {
    idleThresholdMs: 3 * 1000,        // 여기 수정
    longActiveThresholdMs: 10 * 1000, // 여기 수정
  },
  // ...
};
```

---

## 🔍 테스트

### Idle 테스트 (dev 모드)
1. 앱 실행
2. **3초 동안 입력 안 함**
3. 채팅창에 메시지 표시 확인

### 장시간 활동 테스트
1. **10초 동안 마우스 계속 움직이기**
2. 콘솔에 카운트다운 표시
3. 메시지 표시 확인

---

## 🐛 콘솔 로그

```
🔍 Activity Monitor 시작 (dev 모드)
👂 입력 이벤트 리스너 등록
⏱️  장시간 활동까지 8초 남음
😴 Idle 상태 진입
⏰ 장시간 활동 감지
```

---

## 📝 커스터마이징

메시지나 모션을 바꾸려면 `index.html` 353-377번째 줄 수정:
```javascript
onIdle: () => {
  addMessage('assistant', '여기 메시지 수정');
  model.motion('Idle'); // 여기 모션 변경
},
```

